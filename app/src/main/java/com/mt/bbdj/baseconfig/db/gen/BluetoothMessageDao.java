package com.mt.bbdj.baseconfig.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.mt.bbdj.baseconfig.db.BluetoothMessage;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BLUETOOTH_MESSAGE".
*/
public class BluetoothMessageDao extends AbstractDao<BluetoothMessage, Void> {

    public static final String TABLENAME = "BLUETOOTH_MESSAGE";

    /**
     * Properties of entity BluetoothMessage.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property BluetoothMessage = new Property(0, String.class, "bluetoothMessage", false, "BLUETOOTH_MESSAGE");
    }


    public BluetoothMessageDao(DaoConfig config) {
        super(config);
    }
    
    public BluetoothMessageDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BLUETOOTH_MESSAGE\" (" + //
                "\"BLUETOOTH_MESSAGE\" TEXT);"); // 0: bluetoothMessage
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BLUETOOTH_MESSAGE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BluetoothMessage entity) {
        stmt.clearBindings();
 
        String bluetoothMessage = entity.getBluetoothMessage();
        if (bluetoothMessage != null) {
            stmt.bindString(1, bluetoothMessage);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BluetoothMessage entity) {
        stmt.clearBindings();
 
        String bluetoothMessage = entity.getBluetoothMessage();
        if (bluetoothMessage != null) {
            stmt.bindString(1, bluetoothMessage);
        }
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public BluetoothMessage readEntity(Cursor cursor, int offset) {
        BluetoothMessage entity = new BluetoothMessage( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0) // bluetoothMessage
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BluetoothMessage entity, int offset) {
        entity.setBluetoothMessage(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(BluetoothMessage entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(BluetoothMessage entity) {
        return null;
    }

    @Override
    public boolean hasKey(BluetoothMessage entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
